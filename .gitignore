# Original full-resolution source images dropped in the repo ROOT.
# Web-optimized copies live in /images (those ARE committed).
# Anchored to root so images/** is never affected.
/*.png
/*.jpg
/*.jpeg
*.psd
*.xcf

# OS / editor junk
.DS_Store
Thumbs.db
desktop.ini
*.swp
.idea/

# Tooling / agent config
.claude/

# Node (only if you ever add tooling)
node_modules/
npm-debug.log*
